import React, { useEffect } from "react";

const Filters = () => {
  useEffect(() => {}, []);

  return (
    <div className="container">
      <h1>Filters</h1>
    </div>
  );
};

export default Filters;
