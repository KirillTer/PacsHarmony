import React, { useEffect } from "react";

const LauncherFilters = () => {
  useEffect(() => {}, []);

  return (
    <div className="container">
      <h1>LauncherFilters</h1>
    </div>
  );
};

export default LauncherFilters;
