import React, { useEffect } from "react";

const ExamCodes = () => {
  useEffect(() => {}, []);

  return (
    <div className="container">
      <h1>Exam Codes</h1>
    </div>
  );
};

export default ExamCodes;
